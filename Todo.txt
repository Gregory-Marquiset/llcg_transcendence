PROMETHEUS :
    -1ère routes backend:
        -Metrics applicatives via la lib client Prometheus dans ton backend:
            -requêtes/s par route, latences (histogram), taux d’erreurs.
            -compteur utilisateurs connectés (si WS), file d’attente matchmaking, etc.
        -Dashboards “API” (latences P95/P99, erreurs par route).
        -Alertes : P95 > 500 ms sur 5 min, erreurs > 2% sur 5 min.
    
    -Ajout base de données:
        -MySQL (ou Postgres) Exporter:
            -connexions utilisées/max, requêtes lentes/s, locks, buffer pool.
        -Alertes DB : connexions > 80% du max, lentes/s > seuil pendant 5 min.

    